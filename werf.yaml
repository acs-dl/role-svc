configVersion: 1
project: 'backend'
---
image: builder
from: golang:1.19-alpine
docker:
  WORKDIR: /go/src/gitlab.com/distributed_lab/acs/role-svc
git:
- add: /
  to: /go/src/gitlab.com/distributed_lab/acs/role-svc
  stageDependencies:
    setup:
    - "**/*"
shell:
  setup:
  - export CGO_ENABLED=0
  - export GO111MODULE=off
  - export GOOS=linux
  - go build  -o /usr/local/bin/role-svc  /go/src/gitlab.com/distributed_lab/acs/role-svc

---
image: service
from: alpine:3.9
docker:
  ENTRYPOINT: role-svc
shell:
  setup:
  - apk add --no-cache ca-certificates
import:
- image: builder
  add: /usr/local/bin/role-svc
  to: /usr/local/bin/role-svc
  after: setup
